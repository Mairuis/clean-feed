{
  "name": "clean-feed",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@10.30.3",
  "scripts": {
    "build": "node scripts/build.mjs",
    "check": "tsc --noEmit",
    "test": "vitest run",
    "validate": "npm run check && npm run test && npm run build && node scripts/validate-manifest.mjs"
  },
  "dependencies": {
    "@ai-sdk/anthropic": "^3.0.71",
    "@ai-sdk/openai-compatible": "^2.0.41",
    "ai": "^6.0.168",
    "zod": "^4.3.6"
  },
  "devDependencies": {
    "@types/chrome": "^0.1.0",
    "esbuild": "^0.28.0",
    "typescript": "^6.0.3",
    "vitest": "^4.1.4"
  }
}
